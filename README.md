# ai-test-orchestrator

AI-powered pipeline that:
- Generates Playwright tests using LLMs
- Refactors them into Page Object Model
- Executes tests automatically
- Analyzes results

## Activate .venv

```bash
. .venv/Scripts/activate
```

## Run

```bash
python orchestrator.py # OR
npx playwright test # to run the tests
```